{% extends 'base.html.twig' %}

{% block title %}Edit GtUser{% endblock %}
{% block activeuser %}active{% endblock %}
{% block section %}
    {{ form_start(form) }}
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">dispatch</h3>
        </div>
        <!-- /.card-header -->
        <!-- form start -->

        <form>
            <div class="card-body">
                <div class="image">
                    <img src="{{ img_folder}}/{{ gt_user.gtImage }}" class="img-circle elevation-2" alt="User Image" style="width: 40px; height: 40px">
                </div>
                <div class="info">
                    <h6 href="#" class="d-block">{{gt_user.fullName }}</h6>
                </div>

                <div class="form-group">
                    <label for="exampleInputEmail1">Tache à faire</label>
                    {{ form_widget(form.taskToDO,{'attr': {'class': 'form-control'}}) }}
                </div>
            </div>


            <!-- /.card-body -->

            <div class="card-footer">
                {{ include('gt_user/_form.html.twig', {'button_label': 'Update'}) }}

                <a class="btn btn-info btn-sm "href="{{ path('app_gt_user_index') }}">Back to list</a>
            </div>

        </form>
    </div>
    {{ form_end(form) }}

{% endblock %}
